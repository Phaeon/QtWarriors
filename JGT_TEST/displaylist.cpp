#include "displaylist.h"

void display()
{
    glClear(GL_COLOR_BUFFER_BIT);
    /*
    glColor3f(1.0, 1.0, 1.0);
    glCallList(1);
    glTranslatef(-1.1, 0.0, 0.0);
    glCallList(1);
    */
    glFlush();
}
